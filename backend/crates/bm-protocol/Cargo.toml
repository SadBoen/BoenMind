[package]
name = "bm-protocol"
version = "0.1.0"
edition = "2024"
description = "BoenMind 契约层：内核/插件/存储共享的纯类型（零运行时依赖，仅 serde）"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
