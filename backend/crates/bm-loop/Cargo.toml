[package]
name = "bm-loop"
version = "0.1.0"
edition = "2024"
description = "自研 ReactLoopAgent（主线 A6）：turn/step 双层循环 + 五扩展点 + 工具注册表（骨架）"

[dependencies]
bm-protocol = { workspace = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
