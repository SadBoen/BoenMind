# BoenMind 前端（v3 重写）

React 19 + Vite 8 + Tailwind v4 + daisyUI 5 + react-resizable-panels。按 `docs/FRONTEND-GUIDE.md` v3 实现：自建 Unit + 主题两轴（风格 × 材质），不用 dockview。

```bash
cd frontend
npm install
npm run dev
```

打开 http://127.0.0.1:5173
