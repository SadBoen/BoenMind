export default function AccountSection() {
  return (
    <div className="empty">
      <div>鉴权未启用</div>
      <p style={{ color: "var(--fg-3)" }}>装配鉴权后会显示启动全屏登录（密码框 + 进入）。本版不做登录界面。</p>
    </div>
  );
}
