/** Host-visible node half for the persistent Task DAG client plugin. */
export const name = 'task-dag'

/** The task graph is projected entirely from durable browser Session state. */
export function apply() {}
