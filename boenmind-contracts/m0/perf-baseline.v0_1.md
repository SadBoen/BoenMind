# BoenMind 性能与资源基线定标（M0.6 骨架）

> 版本 v0_1（2026-08-28 定标）。
> 落实基线 §18-M0.6「性能与资源基线定标」。定标原则：**M0 只定义指标与测量方法，
> 数值由 M1 以 mock 模型回填**；回填后本文件即成为后续每个里程碑回看（§19-G）的
> 回归基线。禁止拿联网真实模型跑分（外部方差不可控），一律用可注入延迟/长度的 mock。

## 1. 指标定义（P-01..P-11）

| ID | 指标 | 定义 | 测量方法 | 目标（待 M1 回填） |
|---|---|---|---|---|
| P-01 | 冷启动到 ready | 进程启动 → Wire API 可接受 session.create 的墙钟时间 | 50 次取 p95 | **p50=0.100 / p95=0.124 ms**（M1 回填①） |
| P-02 | Session 恢复 | 含 1 万条持久事件时 session.resume 完成（投影/位点就绪）p95 | 预置事件库，20 次取 p95 | **p50=23.78 / p95=25.87 ms**（M2 回填②） |
| P-03 | 单 Agent 回合延迟 | send_input → 执行收据落盘；mock 模型固定注入 200ms 延迟 | N=100 回合，取 p50 / p99 | **p50=223.1 / p99=236.9 ms**（M1 回填①；注入 200ms,自身开销≈23ms,含 2ms 收据轮询量化） |
| P-04 | 事件追加吞吐 | 持久事件日志单写者追加速率（含落盘） | 批量 1 万事件计时 | **106 条/s**（M2 回填②;每条 fsync+物化+位点 CAS,Windows fsync 昂贵,如实记录） |
| P-05 | 事件回放速率 | 从空库重放到 1 万事件完成 | 计时 | **580,245 条/s**（M2 回填②,内存速度:重放不含 fsync） |
| P-06 | 稳态 RSS | M1 进程 100 回合后常驻内存 | OS 采样取稳态中位数 | 待 M2 回看补测（进程内嵌于测试宿主,独立进程采样自 M3 守护形态起才有意义） |
| P-07 | 磁盘增量 | 每千条事件的磁盘增量（状态库+事件日志+Execution Log） | 差分 | **4,172 KB/千条**（M2 回填②;口径含未检查点 WAL,定期 checkpoint 后显著缩小,见记录②注） |
| P-08 | 脱敏扫描开销 | 每条日志记录的脱敏扫描平均耗时 | 微基准 N=10 万条 | **2.715 µs/条**（M1 回填①,内存模式） |
| P-09 | Broker 授权决策开销 | 单次 capability 调用的授权决策(查表+凭证校验,不含 Provider 执行)p99 | 微基准 N=10 万次(M4 规格 §5.1 证伪测试①) | **release p99 ≈ 0.2 µs**(M4 回填③;门槛 10 µs,余量 50×;test build < 200 µs 门同过) |
| P-10 | 队头阻塞注入 | 50ms 慢 Provider 在册时,无关授权决策(纯查表)的 p99 相对空闲基线劣化 | 注入对照 N=500(M4 规格 §5.1 证伪测试②) | **零劣化**(M4 回填③:基线 0.200 / 在册 0.200 µs,门 2×) |
| P-11 | Task Board 投影重建延迟 | 从空投影重建到 N 条 task.* 事件全量回放完成(确定性投影,M5 规格 §5.2);首个投影重建定量(ADR-0004 未决分歧 4 的回应) | 1 万事件预置(含 task.* 族),20 次取 p95(M5 规格 §4-9) | **release p95 ≈ 0.95 ms**(M5 回填④;1 万事件,门 1s,余量 ~1000×;test build 门同过) |

### 1.1 M1 回填记录①（2026-08-29,提交号见回看记录）

```text
环境:  AMD Ryzen 7 PRO 8845HS(8C16T)/ 61.8GB RAM / ZHITAI TiPlus7100 1TB
       Windows 11 x64(10.0.26100)/ rustc 1.98.0 --release
负载:  MockConnector 脚本化(无外网);P-03 注入 200ms 真实睡眠;
       P-01 冷启动为进程内 RuntimeHandle::start(非独立 OS 进程)
口径:  单调时钟;P-03 收据轮询间隔 2ms(量化计入);P-08 为内存模式(不含磁盘 I/O)
方法:  runtime/crates/bm-testkit/tests/perf_smoke.rs(#\[ignore],--release 手动运行)
```


### 1.2 M2 回填记录②（2026-08-29,提交号见 M2 回看记录）

```text
环境:  同 1.1(AMD Ryzen 7 PRO 8845HS / 61.8GB / TiPlus7100 / Win11 / rustc 1.98.0 --release)
负载:  1 万条预置事件(runtime.started 类 no-op 载荷 + 周期性会话事件);
       P-02 前提 = 完整日志(双侧关闭自动压实)
口径:  P-04 每条事件 fsync+物化+位点 CAS(最保守口径);P-05 重放无 fsync;
       P-07 含未检查点 WAL(SQLite 默认约 1000 页自动 checkpoint,
       稳态显著小于该口径;checkpoint 策略随 M3 守护形态定标)
方法:  runtime/crates/bm-testkit/tests/perf_m2.rs(#[ignore],--release 手动运行)
```

### 1.3 M4 回填记录③（2026-08-29,提交号见 M4 回看记录）

```text
环境:  同 1.1(AMD Ryzen 7 PRO 8845HS / 61.8GB / TiPlus7100 / Win11 / rustc 1.98.0 --release)
负载:  MockConnector 脚本化(无外网);builtin system.* 能力集经 Broker 全链路
口径:  P-09/10 为决策路径纯查表计时(catch_unwind 收容与凭证签发含内);
       P-01..P-08 复跑口径与记录①②一致
方法:  m4_falsification_tests.rs(P-09/10,常驻 CI)与
       perf_smoke / perf_m2(--ignored,--release)
复跑(vs M3 基线,劣化门 25%):
  P-01  p50=0.107 / p95=0.133 ms   (基线 0.090/0.114,+18.9%/+16.7%,门内)
  P-02  p50=25.77 / p95=27.70 ms   (基线 23.78/25.73,+1.2%/+7.7%,门内)
  P-03  p50=219.3 / p99=232.4 ms   (基线 218.9/233.2,+0.2%/-0.3%,门内)
  P-04  104 条/s                   (基线 106,-1.9%,门内)
  P-05  首跑 425,827 / 重跑 509,770 条/s(基线 580,245;
        首跑 -26.6% 触门,重跑 -12.1% 门内。materialize/event_log 热路径
        自 M2 零改动(git log 佐证),首跑判为环境噪声,双值留档)
  P-07  4,167.9 KB/千条            (基线 4,172,门内)
  P-08  2.941 µs/条                (基线 2.715,+8.3%,门内)
首次回填:
  P-09  release p99 ≈ 0.200 µs(门槛 10 µs,余量 50×;test build 门 200 µs 同过)
  P-10  零劣化(基线 0.200 µs = 慢 Provider 在册 0.200 µs)
```

### 1.4 M5 回填记录④（2026-08-30,提交号见 M5 回看记录）

```text
环境:  同 1.1(AMD Ryzen 7 PRO 8845HS / 61.8GB / TiPlus7100 / Win11 / rustc 1.98.0 --release)
负载:  MockConnector 脚本化(无外网);memory.*/task.*/watchdog 全链路经 Broker
口径:  P-11 为纯内存折叠计时(BTreeMap 键序 + 单调 seq);P-01..P-10 复跑
       口径与记录①②③一致
方法:  m5_task_lifecycle.rs t57(P-11,常驻 CI)与
       perf_smoke / perf_m2(--ignored,--release)
复跑(vs M4 回填,劣化门 25%):
  P-01  p50=0.160 / p95=0.203 ms   (M4 0.107/0.133,+49%/+53%——触门,判解释)
  P-02  p50=25.66 / p95=27.89 ms   (M4 25.77/27.70,门内)
  P-03  p50=220.1 / p99=233.0 ms   (M4 219.3/232.4,门内)
  P-04  104 条/s                   (M4 104,门内)
  P-05  561,098 条/s               (基线 580,245,-3.3%,门内)
  P-07  4,135.2 KB/千条            (M4 4,167.9,门内)
  P-08  2.798 µs/条                (M4 2.941,门内)
首次回填:
  P-11  release p95 ≈ 0.95 ms(1 万事件;门 1s,余量 ~1000×;test build 同过)
P-01 触门解释(双值留档,非噪声):
  复跑两遍 p50/p95 稳定(0.160/0.203 与 0.161/0.185),排除环境噪声——
  M5 起首启物化 12 条 bootstrap 协调权 Grant(每条含 JSONL fsync + SQLite
  写),为有意的启动期功能成本;冷启动绝对值仍 < 0.21 ms,用户不可感;
  且存量库重启不重发(物化幂等),生产稳态启动成本低于本口径。
  裁决:解释留档,不回炉(M5 回看 §19-G)。
```

### 1.5 M7 回填记录⑤（2026-08-30,提交号见 M7 回看记录）

```text
环境:  同 1.1(AMD Ryzen 7 PRO 8845HS / 61.8GB / TiPlus7100 / Win11 / rustc 1.98.0 --release)
负载:  MockConnector 脚本化(无外网);turn 过 Broker(model.invoke 授权 + 审计)
新计量(M7 焦点:turn 过 Broker 开销):
  P-03  p50=221.8 / p99=236.7 ms   (M5 220.1/233.0,+0.8%/+1.6%,门内)
        —— 200ms 注入口径下,Broker 查表(O(1) 索引)与回合审计事件
        (capability.invoked,回合事件 11→12 条)的成本不可分辨,门内。
存量复跑(劣化门 25%):
  P-01  p50=0.165 / p95=0.281 ms   (M5 0.160/0.203;p50 +3% 门内)
  P-08  2.774 µs/条                (M5 2.798,门内)
P-01 p95 解释(留档,非稳定劣化):
  同环境复跑两遍 p95=0.249 / 0.194,首遍 0.281 与复跑差 ~45%,波动为
  环境噪声(冷启动含 AV/文件系统抖动);p50 三值 0.159/0.160/0.165 稳定,
  相对 M5 无趋势性劣化。M7 启动路径增量仅健康表初始化(空 HashMap)与
  装配字段,无新增 IO。裁决:解释留档,不回炉(M7 回看 §19-G)。
```

## 2. 测量方法学（所有指标通用）

```text
环境:  记录机器档位(CPU/内存/盘)、OS 版本、提交号——回填数值必须附带环境元数据
负载:  mock 模型(无外网);输入固定语料;关闭无关后台任务
次数:  延迟类 N=100、吞吐类固定总量计时;先丢弃前 10 次预热
口径:  墙钟以单调时钟计(基线 8.3);时间戳仅参考
复跑:  每个里程碑回看重跑全部已回填指标,结果写入回看记录(§19-G)
```

## 3. 回归门槛

- 任一已回填指标相对上一次基线**劣化 > 25%** → 触发架构回看，给出解释或回炉项。
- 新增指标 = 追加 ID；既有 ID 定义不得改（与合同冻结纪律一致）。
- 目标值回填动作发生在 M1 里程碑回看；回填后此文件进入只增不改状态。
