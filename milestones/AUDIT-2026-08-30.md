# 审计台账(2026-08-30 起)

- 纪律:**只记录,不修改**。所有审计发现(浏览器实测 / CLI 场景实测 /
  外部审计报告)统一入账,攒批处理。
- 格式:编号 | 来源 | 严重度(高/中/低) | 现象 | 根因(如查明) | 建议修复 | 状态

## 待处理

| 编号 | 来源 | 严重度 | 现象 | 根因 | 建议修复 | 状态 |
|---|---|---|---|---|---|---|
| A-01 | 浏览器实测(用户) | 高 | 回合 succeeded 但回答正文无处可见 | model.invocation.completed 载荷不含 content,正文零落地 | 事件载荷 +content/content_truncated(Minor) | **已修复** 59c6bc6 |
| A-02 | 浏览器实测(用户) | 高 | 服务器正常,页面永远显示「未连接」 | conn 探测错用不存在的 operation,必然失败 | 改用 task.list 探测 | **已修复** 4315129 |
| A-03 | 浏览器实测(代理) | 高 | 事件流零接收(回答/失败事件全部丢失) | EventSource 无法携带 Authorization 头,/events 被 401 静默拒绝 | 前端改走 events.poll 轮询 | **已修复** 4315129 |
| A-04 | 浏览器实测(代理) | 中 | 前端读不到事件类型(全部 undefined) | 事件信封 JSON 字段名为 type(serde rename),前端误读 event_type | 前端改读 type | **已修复** 4315129 |
| A-05 | 浏览器实测(代理) | 高 | 页面所有按钮静默失效(无任何报错) | 内联脚本缺右括号 → 整段脚本解析失败 | 修复括号;脚本改动后强制 node --check | **已修复** 4315129 |
| A-06 | CLI 网页终端实测 | 中 | 含 JSON 参数的命令全部失败(--args 须为 JSON 对象) | 服务器端参数切分不处理 \" 转义,JSON 被引号切碎 | 切分器支持反斜杠转义,或 CLI 侧支持 --args @file | 待处理 |
| A-07 | CLI 场景实测(审计员本人即抄错) | 低 | 跨命令传递 session/agent/op 全靠手抄 26 位 ID,极易抄错 | CLI 无「最近会话」速记(如 --last)或 shell 补全 | 增加最近会话速记或输出精简 ID 模式 | 待处理 |
| A-08 | CLI 场景实测 | 低 | events.poll 返回 has_more:true 但不提示续拉方式,且整包输出很长 | 分页语义未在输出面表达 | 输出尾部提示「--since <last_seq> 续拉」 | 待处理 |
| A-09 | CLI 场景实测 | 低 | 回合取消需与真实模型竞速(2-5s 完成),手速难赶上 | 取消仅对长调用实用 | 文档标注适用场景;或提供可配置的慢速测试通道 | 待处理 |
| A-10 | 基线口径核对 | 低 | 基线称「交互式 TUI」为阶段一交付面,实际未实现(仅命令行) | M3 交付口径与实现偏差,review 未点破 | AGENTS.md 已如实记档;TUI 随阶段二规划裁决 | 已留档 |

(后续审计发现按序追加:A-11、A-12……)

## 外部审计报告合并(2026-08-30,Archify @02fa71b;已逐项复核确认)

| 编号 | 优先级 | 现象 | 复核方式 | 状态 |
|---|---|---|---|---|
| X-01 | P1 | Wiki App 符号链接越界写(page.write/read 跟随 symlink) | 源码审计一致 | **已修复**(lstat 拒链 + realpath 包含校验,读写同查;t123 验收) |
| X-02 | P1 | SSE 未按 session_id 过滤,跨会话事件泄露(实测:单会话流混入 8 会话 60+ 事件) | 在线取证 + 源码一致 | **已修复**(流内过滤,全局事件不进会话流;t121 双会话验收) |
| X-03 | P1 | 压实后损坏恢复静默生成不完整状态(open_resilient 无前缀检查,注释承诺未兑现) | 源码矛盾确认 | **已修复**(fail-closed:首序号>1 拒绝自动重建要求快照;t119e 验收) |
| X-04 | P2 | 在线备份非原子(checkpoint 与 copy 间有提交窗口;events.jsonl 位点不绑定) | 源码确认 | **已修复**(SQLite Online Backup API + events.jsonl 同拷 + sha256 位点清单;t118 篡改拒绝验收) |
| X-05 | P2 | MCP readOnly+destructive 冲突标注降级为免审批只读 | 源码确认(readOnly 分支先行) | **已修复**(destructive 优先,元数据只能提风险;冲突单测验收) |
| X-06 | P3 | Market qty=true 被接受为 1(bool 是 int 子类) | 源码确认 | **已修复**(严格 type(qty) is int;t124 验收) |

裁决:按审计给定顺序修复(①Wiki→②SSE→③fail-closed→④原子备份→⑤MCP
冲突→⑥qty),每项带验收测试;完成后全量门禁 + 回归。

## 实测通过项(2026-08-30,网页 CLI 终端)

S1 会话生命周期 ✓ · S2 真实模型问答 ✓(4688 入/25 出,回答完整)
· S3 直通 ✓(无参形态) · S4 审批拒绝流 ✓(waiting_user→denied)
· S5 受 A-06 阻塞 · S6 任务生命周期 ✓(running→paused→running→cancelled)
· S7 取消受理 ✓ · S8 事件轮询 ✓ · S9 未知能力默认拒绝 ✓ · S10 help ✓


## 第二份审计复核(LLM 审计,2026-08-30;裁决原则:属实且符合架构才修)

| 编号 | 审计主张 | 复核结论 | 证据 | 裁决 |
|---|---|---|---|---|
| L-01 | SSE 150ms 轮询惊群 / 全文件扫描 / 无广播 | **属实**(replay_since 自文件头逐行扫,连接数×文件大小放大) | event_log.rs:53 | 属架构内既定权衡(M3「服务端零订阅状态」明文设计;阶段一单用户;UI 已改走 1.5s events.poll)→ **登记演进项,不重写**(引入广播总线需新 ADR) |
| L-02 | 无快照致冷启动单调递增 | **部分不实**——正常冷启动直接载 SQLite 不重放;compact/snapshot 机制存在;backup_into 即全量快照 | store.rs/恢复路径 | 缺的是「定期自动快照」自动化 → 登记演进项 |
| L-03 | std Mutex 阻塞 Tokio | **属实**(形态) | sqlite_state.rs | 阶段一规模可接受的既定取舍 → 登记演进项 |
| L-04 | unwrap 102 / expect 287 | **计数属实**;但「锁未中毒」等 expect 是文档化 fail-fast 约定(兜底=L0 重启,M4 裁决) | 全仓 grep | 不做全量清扫;登记演进项(逐步收敛 BmError) |
| L-05 | MCP 子进程孤儿/无沙箱 | **属实**(kill_on_drop 缺失;无资源配额) | mcp.rs spawn | **已修 kill_on_drop**;OS 级沙箱(配额/pdeathsig)登记演进项 |
| L-06 | 前端 innerHTML XSS | **属实**——task.title 用户输入未转义直拼(log() 仅转义 `<`) | index.html:111/191/214 | **已修**:全部改 createElement+textContent |
| L-07 | 合同 JSON + Rust 双重维护 | 属实观察,但为 M0 冻结的「合同语言中立 + sync 后验对账」既定决策 | M0/合同冻结纪律 | **不予理会**(设计选择);代码生成器属架构演进需新 ADR |
| L-08 | runtime.rs 5582 行上帝文件 | **属实**,但单写者单文件是 AGENTS.md 明文纪律(单点合并成本>收益) | AGENTS.md 提速方案 | **已完成**(2026-08-30 用户拍板即拆:机械移入 runtime/ 七子模块 cmd/handle/handlers/provider_health/task_ops/turn/tests;行为零变化,clippy 全绿 + 235 测试全绿) |
| L-09 | 前端简朴与后端严谨脱节 | 属实,但 M8 规格 S7 明文「零构建链单页为限」 | M8 规格 | **不予理会**(范围既定);框架化随阶段二 |

## 裁决汇总

- **已修**:L-05(kill_on_drop)、L-06(XSS textContent 化)
- **属实+架构内权衡/演进项,登记缓办**:L-01、L-02(自动化)、L-03、L-04、L-08、L-09
- **不实/部分不实**:L-02 前半(冷启动不重放)、L-09(范围既定)
- **设计选择,不予理会**:L-07

## 第三份评估复核(LLM「4.2 改进建议」,2026-08-30;裁决原则同前)

| 编号 | 评估主张 | 复核结论 | 证据 | 裁决 |
|---|---|---|---|---|
| E3-1 | runtime.rs 5,583 行上帝文件,按职责拆子模块 | **属实**(实测 5,582 行,占 bm-core 全 crate 53%) | `wc -l`(本日) | 即 L-08 → **已拆**(同日用户拍板「现在专门拆一轮」,当日完成) |
| E3-2 | Web UI 缺前端工程化(SPA/TS/构建链) | 属实观察;用户 2026-08-30 明示「前端先放一边不管」 | 用户指示 | **搁置**(用户裁决);维持 M8 规格 S7 零构建口径 |
| E3-3 | Tauri 壳仅骨架未深度集成 | **属实且已留档**(tauri-cli 未装,骨架+复现命令;解除条件=装 CLI 出一次包) | M8-review §6-1 | 待用户裁决(阶段二首批/立即补/不要壳) |
| E3-4 | perf-baseline P-01..P-08 为骨架、需真实运行回填 | **基本不实**——P-01..P-05、P-07..P-11 十项均已实测回填(回填记录①..⑥,含环境元数据与 25% 劣化门,M1 起逐里程碑复跑);唯一未回填 = **P-06 稳态 RSS**(原注:独立进程采样自 M3 守护形态起才有意义) | perf-baseline.v0_1.md 记录①..⑥ | 纠正记录;P-06 补测登记:随阶段二「真实使用一周」对常驻 server 采样回填(真实负载口径,不为此专造 mock 守护形态) |
| E3-5 | 阶段一遗留项(S4 draining/lease/worker 自主环/流式/memory:user)需阶段二优先处理 | 定性属实;**非「信息不足待拍板」而是既定策略**——阶段一范围冻结,六项均留档(M8-review §6-4/§7-6),既定路径 = 先真实使用一周、按使用反馈定阶段二优先级 | M8-review §6-4、§7-6 | 维持既定策略;哪些提前属用户偏好,PENDING 已补大白话裁决项(D-M8-1) |

## 管理方法借鉴登记(addyosmani/agent-skills 对照,2026-08-30)

对照结论:该仓 24 技能方法论主线(spec→plan→build→test→review→ship)与既有
治理约八成同构且我方更严(spec 冻结、约束驱动、怀疑驱动=审计台账、真实浏览器
测试均已内化);**整体引入会造成 AGENTS.md→boenmind-dev 双源漂移(同 L-07
理由),不引入**。择三小件入册:

1. **observability-checklist**:可观测性(指标/告警)为真实空白(仅事件日志
   +tracing);阶段二起草可观测性规格时作对照清单防漏项。
2. **Chesterton 栅栏原则**(code-simplification):约束 E3-1/L-08 拆分施工——
   拆前逐行弄懂为何存在;拆分限纯机械搬移,行为零变化,以 235 测试+黄金轨迹
   +clippy 全量回归把关。
3. **accessibility-checklist**:web UI 无障碍现状为零,登记不修(前端冻结中,
   同 E3-2);随阶段二前端工程化一并裁决。
