# 外部复盘报告复核存档 — 2026-09-07(基线 v0.0.13)

> 来源:用户转交的外部「资深代码架构师」全方位复盘与重构方案报告(自称三探索代理并行,基线 v0.0.13)。
> 本文件只做**逐项复核结论存档**;欠账登记唯一入口仍是 `milestones/BACKLOG.md`。
> 复核方式:逐项对照 main 源码核实,规程依据:审计报告处理流=逐项复核属实再修,严重度自己重估。

## 一、总结论

1. **属实已修 4 件**:skills.json 损坏静默清库隐患(数据安全,报告全场最有价值发现)、
   roles.json 同族隐患(报告漏列,复核时顺藤摸出)、read_mcp_servers 逐字重复、
   嵌套错位空目录;**文档校准 3 处**(apps/ Python 口径、stream 必带过时说法、插件手动批准过时流程)。
2. **属实但已登记 BACKLOG(不擅动)6 项**:rusqlite 泄漏核心(F-12)、webadmin 直接装配(F-07)、
   400ms 审批轮询、记忆能力生产零装配、OS 级沙箱(候选队列)、context.tsx 大文件(P3)。
3. **误报/误判驳回 8 项**(证据见 §四),其中「基线 §78 第一公民长期记忆」系**编造引文**。
4. 报告整体质量:事实层大多准确(file:line 基本命中),但定性层普遍把「刻意设计+注释自证」
   的选型判成补丁/污染,且存在编造文档引文一处;严重度按本仓惯例逐项重估后入账。

## 二、修复明细(本批落地)

| # | 报告条目 | 复核 | 处置 |
|---|---|---|---|
| 1 | 冗余2: read_skills 损坏静默回落空 Vec | **属实,高危**。skills.rs:15-21 全链 `.ok()`+`unwrap_or_default()`,盘上 skills.json 半损坏时 GET 静默显空库、下一次 skills_set/skills_delete 以空数组整库覆写=不可逆数据损毁;providers.rs 同位函数是「损坏 500 拒绝覆写」正确口径,四组配置两套标准 | 已修:read_skills Result 化(损坏/缺 skills 数组=500 拒绝,缺文件=空库不变),三调用点全部接错;补回归测试 t_w2_skills_roundtrip_and_corrupt_rejects_overwrite(损坏拒绝加载+拒绝覆写+盘上原样) |
| 2 | (报告漏列,同族顺藤)read_roles_doc 损坏静默回退默认文档 | **属实,同高危**。roles.rs:29-66 对损坏 JSON 一律回退默认单角色文档,下一次 roles_set 即把用户全部角色覆写抹掉 | 已修:Result 化(损坏/无 roles 数组=500 拒绝;缺文件与空数组保留旧默认回退;旧版单 system_prompt 形态迁移保留),四调用点全部接错;补测试 t_w2_corrupt_roles_json_rejects_load_and_overwrite |
| 3 | 冗余1: read_mcp_servers 两处逐字重复 | **属实**。webadmin/mcp.rs:26 与 bm-providers mcp/supervisor.rs:38 同一 10 行函数逐字相同,surface-http 本就依赖 providers | 已修:webadmin/mcp.rs 删本地副本改 `use bm_providers::mcp::supervisor::read_mcp_servers`,启动装载与管理面读取单源 |
| 4 | 7.3.1: 嵌套错位目录 src/crates | **属实(本地工件)**。runtime/crates/bm-core/src/crates/bm-core/src/broker/tests 五层空目录链存在;git 不跟踪空目录,系本地疏漏不入库 | 已删 |
| 5 | 7.2/§八: 发布包含 apps/*.py vs「无需 Python」宣称 | **事实属实、定性偏差**。打包 apps/ 系 2026-09-03 用户裁决(「包内含 apps/ 真实 App」),不动;但 README:18/INSTALL:4 的「无需 Python」未给 apps/ 例外说明,且 README 包内容清单根本没列 apps/,用户拿到 Python 文件无任何解释 | 已修文档:README 包清单补 apps/(可选演示 App,需自备 Python 3,核心运行不依赖)+两处「无需 Node/Python」加例外括注;release.yml 零改动 |
| 6 | §八: INSTALL「BOEN_MODEL_STREAM=1 当前版本必带,配置文件流式字段尚未接线」 | **属实(文档过时)**。stream 已接线:config_store.rs:168 `file["stream"].unwrap_or(env_stream)`,model.json 优先、env 兜底,server 启动打印来源 | 已修:INSTALL §2 改可选注释(stream 字段优先,env 兜底),§3 启动示例命令移除该变量;PLAYBOOK:16 本就写的「model.json 优先」无误不动 |
| 7 | §八: INSTALL「扫描插件→批准接入→重载」手动流程 | **属实(文档过时)**。ADR-0023(2026-09-06)起随包官方插件首启自动播种批准上线(boenmind-server.rs:192-207 seed_bundled_plugins),手动流程仅用于恢复被卸载/删除(墓碑)的官方插件或自装插件 | 已修:INSTALL §4 改「开箱即用(ADR-0023)」+墓碑恢复说明 |

## 三、属实但已登记 BACKLOG(不动代码)

| 报告条目 | 复核 | 处置 |
|---|---|---|
| 发现1: bm-core 直依赖 rusqlite(StoreError::Sql) | **属实**。bm-core/Cargo.toml:21 与 ports/persist.rs:10-12 命中;系端口层错误枚举内联存储驱动型 | BACKLOG F-12 既有条目(依赖倒置,OPEN 缓办)已覆盖,不重开 |
| 发现2: surface-http 直接文件 IO/装配 McpHub | 属实;但管理面(/admin,壳子私用不入合同)读写自身配置+热装载是其职能,收口已在 supervisor::sync_from_config(报告 §4.1 自己表扬) | BACKLOG F-07 既有条目(装配职责下沉待裁决)已覆盖 |
| 3.2: 审批等待 400ms 轮询冲击单写者 | **属实**。spawn.rs 等待循环 400ms 发 Cmd::GetOperation(上限 300s);watch/通知化是正解,属异步结构改造 | BACKLOG「审批等待轮询改推送 OPEN(低)」既有条目 |
| 3.3: 记忆能力生产零装配 | **事实属实**:memory_capabilities 全仓仅 bm-testkit 三处测试引用,boenmind-server 装配面=model.invoke+exec/job_output+fs.*+skill scripts,M5 记忆 CRUD/FTS5 只活在测试台。但这是「待设计裁决的欠账」而非「断裂事故」:BACKLOG §1 既有「记忆检索自动注入 OPEN(待后续讨论)」 | BACKLOG 该行本批扩口径:生产零装配证据+「能力挂载+检索注入」一并待设计裁决 |
| 4.2: 无 OS 级沙箱(JobObject/seccomp/cgroups) | **属实且为既定范围**:阶段一边界,Broker 审批+env_clear 漂白+只读标注是既定防线(报告自己也列了);OS 沙箱=远期安全执行面议题 | 候选队列「context-mode Rust 后续安全执行面(wasmtime/WASI 或 OS 级沙箱)」既有条目 |
| 5.2 隐患3: context.tsx 2095 行 Fat Component | **属实**(精确 2095 行);8s 轮询+密集字符串处理在案 | BACKLOG P3 大文件拆分既有条目(context.tsx 可拆 TrendChart/TokenWaterGauge/PromptRecipe/FileEffects) |
| 补丁2: SHORT_WIRE_NAMES 硬编码 | **属实但系 ADR-0022 刻意选型**(spawn.rs:200-230 注释自证:OpenAI function.name 正则约束/用户裁决 search→rgrep 防误读/exec 按平台呈 shell 名);内置五件是封闭集,MCP 工具走单下划线转义;别名入 manifest=合同增发,收益低 | 维持现状;「CapabilityManifest aliases 字段」作为 ADR-0022 候补族(§1 工具面远期增强三件)同族记忆,不单列 |

**本批新登记 BACKLOG(§3.3)**:
- 同批 tool_calls 拒绝联动(3.1:串行系单写者刻意语义;但「用户拒绝一个工具后同批其余仍各自过审批执行」是否改「拒绝即取消同批余下」属产品裁决);
- 前端长会话渲染性能(5.2 隐患1/2:无虚拟滚动 DOM 全量堆积+流式逐 token 全文重扫/全树重建,可虚拟列表+节流)。

## 四、不属实/误判,驳回(证据在案,勿重查)

| 报告指控 | 驳回证据 |
|---|---|
| 3.4: Failed→Running「硬性状态重置补丁,打破状态机规范」 | handlers.rs:371-375 注释明示「失败自愈(**合同增发** failed→running,resend_after_failure):回合失败≠agent 死亡,同会话再次发消息即恢复接单;发 agent.resumed 同步投影」——系 2026-09-03 外部评审三轮复核确认交付的合同 Minor 修复(状态机增发 AgentResumed),非绕过规范的补丁;其余状态照旧拒绝 |
| §八: 「CORE-ARCHITECTURE.md:78 宣称第一公民长期记忆与经验」 | **引文编造**。基线 §76-78 是核心概念术语表:78 行=「Skill」定义,77 行 Memory 定义=「按域隔离的持久化经验与事实存储;地址即权限边界,实现可替换」;全基线无「第一公民」字样;§283 反而明示「默认不自动写长期记忆,Task 结束时的提炼由所属 App 显式声明」——克制口径是设计立场,记忆未挂载因此是「待裁决欠账」而非「违背基线承诺的严重未闭环」 |
| 7.1: MockConnector 测试桩编译进发布包=污染 | server.rs:113 注释明示「缺省仍 mock(测试确定性)」——无模型配置时(首启未接线)的刻意兜底,回复固定提示语引导接线(:145-149);另被 12+ 测试文件与 examples 消费。加 cfg 网关需 feature 全链路穿孔且破坏无模型首启,收益几百 KB,不立项 |
| 7.1: InProcMcpServer 未条件编译 | 仅 bm-surface-http/bm-testkit 的**集成测试**消费(tests/ 目录)——跨 crate 集成测试拿不到 #[cfg(test)] 门控,要隔离须 feature 机制(testkit feature)牵动 workspace 全部 dev-deps;~60 行内存桩,零副作用,维持现状 |
| 7.1: builtin.rs 测试工具死代码静态编译进二进制 | 报告自己承认「主入口断开了挂载」:生产装配=production_builtin_capability_set() **仅 model.invoke**(builtin.rs:218 注释自证「杜绝早期单元测试桩污染生产环境模型工具集」);演示七件套(echo/counter/notes/mail/purge)只被自身 #[cfg(test)] 单测与 TestRig 消费;未引用符号由链接器段回收,「死代码进发布包」无实证 |
| 补丁1: 内核硬编码中文「工具纪律」提示词 | 2026-09-06 用户提报的意图门控**软防线**(spawn.rs:288-292 注释自证「F2(用户 2026-09-05 提报)…代码层硬门控涉及语义判定产品设计,另行排期」;BACKLOG §1 第 1 行在案);注入位置(messages[1])与措辞系刻意设计,业务提示词外置化属风格偏好,不立项 |
| 7.3.2: server*.log 未在 .gitignore 声明 | **误报**。.gitignore 已有 server.log / server-*.log / runtime/server_*.log 等多条;`git ls-files | grep .log` 零输出=无日志被跟踪 |
| 3.3: context-log 重建 session_chats「脆弱补丁」 | W5/W9 既有设计:context-log.jsonl 即轨迹源,session_chats 台账(20 轮/24K 截断)由其重建是唯一事实来源,配套 open_resilient 损坏自愈(state.db 损坏从事件日志重建投影同族);「线性扫描冷启动变慢」在个人单机数据量下非现实约束,性能化改造无目标场景 |

## 五、报告事实性勘误

- 引文编造一处:「基线 :78 第一公民长期记忆」(见 §四);
- 「多个测试 Mock 直接参与生产编译」表述失准:生产装配面只有 model.invoke,MockConnector 仅作无模型兜底,演示能力桩不进模型工具集;
- 「审批双通道…2.5s 定时轮询」表扬与「400ms 自旋」批评并存:前者是前端 /admin/approvals 轮询,后者是回合管线轮询,两者机理不同(前者可达性兜底、后者状态等待),报告未区分;
- 拆分后文件行数引用(context.tsx 2095/thread.tsx 930)精确,但 release 声称「React 19.2/Vite 7/TS 5.9」以 package.json 实际为准,未逐字核对(不影响结论)。

## 六、执行记录

- 落地 commit:见 git log(修 4 件+文档 3 处+测试 2 个+BACKLOG 扩记 2 行/新登记 2 行);
- 回归:fmt+clippy --all-targets+cargo test --workspace+validate.py(批末统一执行,见 HISTORY 行);
- 本批未触碰 boenmind-contracts(合同零变更)、未触碰 architecture/(无架构变更);
- 上一轮同族存档:milestones/REVIEW-2026-09-07-v0.0.12-external.md。
