# 外部架构审查报告复核存档 — 2026-09-07(基线 v0.0.12)

> 来源:用户转交的外部「资深代码架构师」全面审查报告(Rust 后端+TS 前端+插件+构建配置)。
> 本文件只做**逐项复核结论存档**;欠账登记唯一入口仍是 `milestones/BACKLOG.md`。
> 复核方式:三线并行探索核实(main @ 6b82a37),规程依据:审计报告处理流=逐项复核属实再修,严重度自己重估。

## 一、总结论

1. **报告 P0 属实已修**:`target-inspector/` 329 文件/121MB 编译产物被 Git 跟踪(commit d521999 出索引)。
2. **三处指控不属实,驳回**:SSE 流无超时 / GLM feature 未定义 / ProvidersPage.tsx 旧文件(证据见 §四)。
3. **「上帝文件」等大重构不随手实施**:webadmin.rs 拆分等登记 BACKLOG,与 round2 重构批(批 5-9)合流。
4. 报告对架构(合同驱动/依赖倒置/单写者事件环/五层 Runtime)与测试分离的正面评价与仓内现状相符;
   其 "P1 clippy" 式严重度普遍偏高,按惯例逐项重估后入账。
5. 本轮附带修复:PluginsPage 兼容层清理、`w2/src/` 空目录残留、demo 二进制迁 `examples/`。

## 二、修复明细(本轮落地)

| # | 报告条目 | 复核 | 处置 |
|---|---|---|---|
| 1 | BUG-1: target-inspector 被 Git 跟踪 | **属实**。329 文件/121MB(磁盘 132M),c03b8c5(2026-09-06)一次性引入;`.gitignore:31-34` 只有 `target/` 与全路径条目,匹配不到 `target-inspector/` 目录名;全仓唯一被跟踪的构建产物目录 | 已修 d521999:追加忽略规则 + `git rm -r --cached`(磁盘保留)。历史中 121MB 保留,filter-repo 改写历史须用户明示(强推+全 clone 失效+tag 重写),登记 BACKLOG 待裁决 |
| 2 | P3/冗余: PluginsPage 兼容层 | **属实**。`w2/PluginsPage.tsx` 仅 2 行 re-export,`SettingsPage.tsx:8` 经 shim;唯一消费方 | 已修:SettingsPage 直连 `./plugins/PluginsPage`,删 shim;附带删除空目录残留 `src/w2/src/w2/plugins/` |
| 3 | BUG-2/TEST-2: demo 混在生产 crate | **部分属实**。`bm-runtime/src/main.rs`(133 行)内联 `mod bm_testkit_replay` 复制 testkit 常量(注释自证有意);`cargo build --release` 多编译一个 demo bin,但不链接进 boenmind-server(packaging/INSTALL.md 只部署后者) | 已修:迁 `runtime/crates/bm-runtime/examples/single_agent_demo.rs`(内容原样+头注释更新),删 src/main.rs;`cargo run -p bm-runtime --example single_agent_demo` 实测回合闭环 succeeded。报告路径笔误:实际在 `runtime/crates/` 非 `runtime/` |

## 三、属实但不属缺陷 → 登记 BACKLOG(不动代码)

| 报告条目 | 复核 | 处置 |
|---|---|---|
| 2.2-P1: webadmin.rs 上帝文件 | **属实**:2985 行(精确命中)/约 90 个顶层函数/10 业务域单文件,无 mod 拆分;一处修正=升级 handler 在 about.rs 不在本文件(webadmin.rs:2633 仅挂路由) | BACKLOG §3.2 P3 行更新(行数 1882→2985 + 按域拆分方案),与 round2 重构批合流 |
| QUALITY-3: thread.tsx 过长 | **属实**:930 行精确;Thread/ApprovalDrawer/UserMessage/AssistantMessage/Composer 五组件同居一文件;工具树本体已抽出(ToolTreeGroup),报告称「工具树在 thread.tsx」不完全准确 | BACKLOG §3.2 P3 行补入 |
| REPEAT-2: 配置读写重复 | **属实**(实为四组):providers/skills/roles/mcp 各一套 file→read→atomic_write 样板;skills 无 write helper(写盘内联重复两处 :1096-1110/:1126-1131),providers 读取 Result 化而 skills 静默回落空 Vec(口径不一) | BACKLOG §3.2 新行(JsonStore 收口) |
| QUALITY-1: 恢复路径 expect | **属实但降级**:14 处(非 15+);`handle.rs:81-94` 注释明示「恢复失败=拒绝启动(宁可拒开)」是设计决策,上一层 `store.load_rows()` 同为 panic 口径,单点改 Result 收益有限、全链路传播波及 start() 签名 | 并入 BACKLOG §3.2 P4 行(非测试 panic/expect 不变量断言族),登记「错误信息可读化」可选小改,不改 fail-fast 语义 |
| 2.2-P2: World ~30 HashMap | 属实;报告自己结论「不急于大重构,等下一个大型状态域加入时再拆」 | 维持现状,BACKLOG P3 行已有同族大文件条目,不单列 |
| FE-1: 前端 API 散落 | **属实**:api.ts(508 行)只覆盖 W2 管理面;W1 运行时侧 8 处裸 fetch(main.tsx:16/runtime.tsx×5/thread.tsx:622/AboutPage.tsx:65) | BACKLOG §3.2 新行 |
| FE-2: 版本号手动同步 | 属实(当前 0.0.12 已对齐:runtime/Cargo.toml:16 与 webapp/package.json:4) | BACKLOG §3.3 新行(release 流程 cargo metadata 提取或 CI 校验) |
| TEST-1: 测试命名不统一 | **属实**(34 文件,m1/m4/m7/m9/w5/w8/gt01/perf/无前缀混用;实际目录 `runtime/crates/bm-testkit/tests/`,报告路径笔误) | BACKLOG §3.3 新行(低;机械改名破坏 git blame,缓办) |
| 8.5: 审批无人裁决时无通知 | 属实(依赖前端轮询 /admin/approvals,v0.0.11 已修可达性) | BACKLOG §3.3 新行(通知机制,产品增强) |

## 四、不属实,驳回(证据在案,勿重查)

| 报告指控 | 驳回证据 |
|---|---|
| QUALITY-2: openai_compat SSE 流无超时保护 | **三层保护俱在**:①流式硬顶 `openai_compat.rs:361` Instant deadline(=limits.stream_hard_cap_ms,默认 900s,W10/ADR-0024 可配),:368-370 超限 break;②空闲 keepalive :372-375(10s);③非流式 deadline :281-285;④模型调用本身有 per-request budget 超时(openai_http.rs:358/480)。`/events/{id}`(sse.rs)无上限系服务端推送长连接设计,断线靠 since_seq 重连。报告所称「HTTP 级连接超时缺失」不成立 |
| 8.2: GLM 适配器「Cargo.toml 未见 feature 定义」 | `runtime/crates/bm-providers/Cargo.toml:31` 已定义 `glm = []`;`lib.rs:15-16` cfg 门控 `glm_http`;模块头注释自证「默认关」。feature-gate 默认关闭是有意设计,非缺陷 |
| 冗余清单: ProvidersPage.tsx 旧文件待删 | **活代码**:`SettingsPage.tsx:7` 导入、:117 渲染,19KB 正式功能页(W2 模型提供商页)。删除即破坏设置中心 |

另:STYLE-1(w1/w2/w3 改语义化目录名)驳回不做——全仓 AGENTS.md/规格/HISTORY/注释均以 W 编号为锚,改名破坏全部文档引用;个人项目无「新人上手」成本,收益为负。REPEAT-1(admin_error)报告自己已确认是函数化收口(123 次调用单一 helper),thiserror 化属风格偏好不立项。

## 五、报告事实性勘误

- 路径体系:报告通篇写 `runtime/bm-*`,实际 workspace 在 `runtime/`,crate 在 `runtime/crates/bm-*`;仓库根无 Cargo.toml。
- 「expect 15+」实为 14;「testkit 测试文件」实际路径 `runtime/crates/bm-testkit/tests/`(34 文件)。
- 升级(§8.6)判为 webadmin 职责之一,实际 handler 在 `about.rs`(报告 §7.1 行数表本身正确)。

## 六、执行补记(2026-09-07 同日,用户两点指示)

1. **webadmin.rs 拆分已落地**(报告 2.2-P1,原列 BACKLOG 与 round2 重构批合流,用户明示提前):2985 行按域拆 11 子模块
   (`webadmin/{mod,providers,mcp,roles,skills,approvals,fs,logs,context,limits,jobs}.rs`),
   纯机械移动零语义变化(66 目标/397 用例与拆分前完全一致);对外五符号路径零破坏;
   BACKLOG P3 行已移出 webadmin。
2. **历史清理已执行**(BUG-1 后续,用户明示「下次推送不带那 121M」):git filter-repo 全历史清除
   `target-inspector`(121MB)与复核时新发现的同族遗留 `target-wms`(web-multisearch 隔离构建产物,
   335MB),main+受影响 tags 强推重写;仓库 pack 132.6MB→26.2MB,新 clone 27M 且构建产物 0 个。
   既有 clone(如有)须重新拉取;GitHub Release 资产与 VPS 升级链路(走资产不走 clone)不受影响。
3. 报告其余 BACKLOG 登记项(§三表)维持原状,随 round2 重构批与后续排期消化。
