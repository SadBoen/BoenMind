[package]
name = "kernel-assembly"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "组合根：装配 PortBag + 边界守卫（依赖只许向下）"

[dependencies]
kernel-contracts = { path = "../kernel-contracts" }
kernel-session = { path = "../kernel-session" }
kernel-llm = { path = "../kernel-llm" }
kernel-tools = { path = "../kernel-tools" }
kernel-storage = { path = "../kernel-storage" }
kernel-loop = { path = "../kernel-loop" }
kernel-supervisor = { path = "../kernel-supervisor" }
serde_json.workspace = true
thiserror.workspace = true

[dev-dependencies]
tokio.workspace = true
chrono.workspace = true
uuid.workspace = true
