[package]
name = "boenmind-desktop"
version = "0.1.0"
edition = "2021"
description = "BoenMind 桌面壳(薄客户端:加载 boenmind-server 的 Web Surface)"

[dependencies]
tauri = { version = "2", features = [] }

[build-dependencies]
tauri-build = { version = "2", features = [] }

[lints.rust]
unsafe_code = "forbid"
