name: release

# Linux 服务器发布线(Linux x86_64 .tar.gz)
on:
  workflow_dispatch:
  push:
    tags: ["v*"]

permissions:
  contents: write

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: dtolnay/rust-toolchain@1.98.0
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            runtime
            plugins/mcp/web-multisearch
            plugins/mcp/context-mode
            plugins/mcp/code-tools
      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: runtime/webapp/package-lock.json

      - name: 前端构建(dist 入包)
        working-directory: runtime/webapp
        run: |
          npm ci
          npm run build

      # 插件进发布包前先过全量测试(测试挂了不发版)
      - name: 插件测试门禁
        working-directory: plugins/mcp/web-multisearch
        run: cargo test

      - name: 插件测试门禁(context-mode)
        working-directory: plugins/mcp/context-mode
        run: cargo test

      - name: 插件测试门禁(code-tools)
        working-directory: plugins/mcp/code-tools
        run: cargo test

      - name: 服务器构建
        working-directory: runtime
        run: cargo build --release --bin boenmind-server

      - name: 插件构建
        working-directory: plugins/mcp/web-multisearch
        run: cargo build --release

      - name: 插件构建(context-mode)
        working-directory: plugins/mcp/context-mode
        run: cargo build --release

      - name: 插件构建(code-tools)
        working-directory: plugins/mcp/code-tools
        run: cargo build --release

      - name: 打包(Linux)
        run: |
          VER="${GITHUB_REF_NAME#v}"
          PKG="boenmind-${VER}-linux-x86_64"
          mkdir -p "${PKG}/webapp" "${PKG}/plugins" "${PKG}/apps"
          cp runtime/target/release/boenmind-server "${PKG}/"
          cp -r runtime/webapp/dist "${PKG}/webapp/dist"
          cp plugins/mcp/web-multisearch/target/release/web-multisearch "${PKG}/plugins/"
          cp plugins/mcp/web-multisearch/README.md "${PKG}/plugins/web-multisearch-README.md"
          cp plugins/mcp/context-mode/target/release/context-mode "${PKG}/plugins/"
          cp plugins/mcp/context-mode/README.md "${PKG}/plugins/context-mode-README.md"
          cp -r plugins/mcp/context-mode/upstream-notices "${PKG}/plugins/context-mode-upstream-notices"
          cp plugins/mcp/code-tools/target/release/code-tools "${PKG}/plugins/"
          cp plugins/mcp/code-tools/README.md "${PKG}/plugins/code-tools-README.md"
          cp apps/*.py "${PKG}/apps/" 2>/dev/null || true
          cp apps/mcp-config.example.json "${PKG}/apps/" 2>/dev/null || true
          cp packaging/INSTALL.md "${PKG}/INSTALL.md" 2>/dev/null || true
          test -x "${PKG}/plugins/web-multisearch"
          test -x "${PKG}/plugins/context-mode"
          test -x "${PKG}/plugins/code-tools"
          tar czf "${PKG}.tar.gz" "${PKG}"
          ls -la "${PKG}.tar.gz"
          # 升级客户端按 <资产名>.sha256 拉取校验文件(about.rs;首 token=hex)
          sha256sum "${PKG}.tar.gz" > "${PKG}.tar.gz.sha256"
          sha256sum "${PKG}.tar.gz" > SHA256SUMS.txt

      - name: 创建 GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VER="${GITHUB_REF_NAME#v}"
          gh release create "${GITHUB_REF_NAME}" \
            boenmind-*.tar.gz boenmind-*.tar.gz.sha256 SHA256SUMS.txt \
            --title "BoenMind ${GITHUB_REF_NAME}" \
            --notes "BoenMind ${GITHUB_REF_NAME} (Linux x86_64).

          包含:
          - boenmind-server: AI OS / Runtime 服务器核心
          - webapp/dist: 最新 Web UI 预构建前端 (含音乐播放器、多角色管理、上下文透视、MCP 管理与完整热插拔等)
          - plugins/web-multisearch: 官方自带聚合搜索 MCP 插件
          - plugins/code-tools: 官方自带代码工具 MCP 插件(查/读/改,rg 引擎内嵌)
          - plugins/context-mode: 官方可选上下文检索/会话连续性 Rust MCP 插件(默认不启用)
          - apps/: 包含音乐播放器 (music_server.py)、Wiki (wiki_server.py)、Market (market_server.py) 等首批真实 App

          支持在网页端 设置 -> 关于 中执行一键在线升级。"
