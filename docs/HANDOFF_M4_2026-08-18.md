# HANDOFF：M3.5 核心收尾完成 + 官方测试集差距登记（2026-08-18）

> 状态：**核心收尾（不碰插件/皮肤）**。settingsNs 热替换（registerAdapter 写面）、
> MAX_STEPS 可配置、keyless 装配、7 处官方 wire 差距修复全部落地并验证。
> 官方测试集 650 spec 已盘点核心面，差距按 P0/P1/P2 登记。
> 上一指针 `docs/HANDOFF_M3_COMPLETE_2026-08-18.md`（M3 完成态）。

---

## 0. 一句话交接

**本轮 = M3 遗留核心项收尾 + 官方测试集启动**。LLM 挂载点差距最后一项（settingsNs
热替换）落地：provider 的 `llm.<id>` settings ns 动态 baseURL + credentials.set 动态 API
key，**写后下一请求即生效**（对齐 DSH dynamic-config.spec.ts，无重启/无重新注册）。
同时 keyless 装配合法化（对齐"starts keyless and serves the next request once the key
arrives"）+ `--max-steps` CLI + 流错误统一 finish 呈现（修掉真 provider 出错时
`Err chunk 覆盖真码 + 双回合收尾`的真 bug）。**下轮指针 = P0 三项 + P1 补形状 +
官方测试集镜像成 Rust 单测**（见 `docs/DSH_TESTSUITE_ALIGNMENT_2026-08-18.md` §3）。

---

## 1. 完成项（本轮）

### settingsNs 热替换（LLM 挂载点差距 3，registerAdapter 写面）
- `kernel-llm::OpenAICompatLlm` 加动态覆盖层（`set_base_url_override`/`set_api_key_override`
  /`effective_*`）；`stream_inner`/`list_models_remote` 用生效值。
- `web-server::settings_write` 支持 `llm.<id>` 动态 ns（`baseURL` 写面 → 同步适配器，
  null/缺省恢复装配值）；`settings_describe` 列出 provider ns。
- `credentials.{set,unset}` 对 `{ID}_API_KEY` ref 同步 key 覆盖（对齐 DSH：key 经
  credentials 每请求解析）。
- **keyless 装配**：provider_config 缺 key 不再跳过，装配为空 key（请求
  MISSING_CREDENTIAL，key 可热补）。
- 验证：`scripts/hot-replace-verify.mjs`（本地 mock SSE 端点 + keyless config）**15/15 PASS**
  ——keyless→MISSING_CREDENTIAL→set key 打 A→settings 改 baseURL 打 B→describe 含 ns。

### MAX_STEPS 可配置（M3.5 遗留 4）
- `kernel-assembly::Runtime.max_steps` 字段 + `headless_with_max_steps`；
  `loop_runtime()` 用之；web-server `--max-steps <n>` CLI（0 拒、帮助文本更新）。冒烟过。

### 官方测试集对齐修复（7 处，对照 DSH spec）
| 修复 | spec 依据 |
|---|---|
| 错误码 HTTP 映射（401/403→AUTH、429→RATE_LIMIT、400→INVALID_REQUEST、500/503→SERVER、其余→HTTP_\<status\>）+ 状态行消息回退 | `adapter.spec.ts` 6 条 |
| 未注册 provider → `NO_ADAPTER`（原 NO_PROVIDER） | `service.spec.ts` |
| assistant tool-call 轮 content 空串（原 null，API 400 拒 null） | `serialize.spec.ts` |
| **流错误统一 finish 呈现**（TRANSPORT/MALFORMED_RESPONSE/STREAM_CLOSED/MISSING_CREDENTIAL/NO_ADAPTER/HTTP 码）——修真 bug：原 Err chunk 被 loop torn 分支覆盖成 LLM_STREAM 且双回合收尾 | `sse.spec.ts`/`translate.spec.ts` |
| keyless → MISSING_CREDENTIAL（finish 呈现，引导语含 credentials service） | `dynamic-config.spec.ts`/`adapter.spec.ts` |

## 2. 验证矩阵（全过）
| 项 | 结果 |
|---|---|
| cargo test --workspace | 67 全过 |
| clippy --workspace --all-targets | 零警告 |
| conformance diff-traces | 17/17 |
| gate25（全新 db） | PASS |
| m3-r3-verify | PASS（40/40） |
| hot-replace-verify（新） | ALL PASS（15/15） |

## 3. 下轮指针（官方测试集主线，不碰插件/皮肤）
见 `docs/DSH_TESTSUITE_ALIGNMENT_2026-08-18.md`：
- **P0**：错误归一化（LlmError 结构化事实 + UNKNOWN 回退）、上下文超限/配额分类、
  abort 语义（kernel-loop cancel 端口）。
- **P1**：stream_options、reasoning_effort/thinking 上 wire、tool-result 哨兵、
  settings-conflict revision、session.search astral。
- **执行化**：把 DSH 官方 `translate.spec.ts`/`sse.spec.ts`/`serialize.spec.ts` 断言
  镜像成 Rust 单测；mock-server 模式复刻 Rust 集成测试（hot-replace-verify 模式已证可行）。

## 4. 纪律（沿用 + 新增）
- 开工/收尾杀残留：`taskkill //F //IM web-server.exe`；临时 db 清理。
- gate25 用全新 db；BM_TEST_HOOKS=1 起测试钩子。
- **新增铁律：新流协议错误一律以 finish 呈现**——Err chunk 会被 loop torn 分支覆盖真码
  （code→LLM_STREAM）且双回合收尾。
- DSH 源码快照 `.tmp/dsh-src`（shallow clone）勿提交；650 spec 盘点产出在
  `docs/DSH_TESTSUITE_ALIGNMENT_2026-08-18.md`。
